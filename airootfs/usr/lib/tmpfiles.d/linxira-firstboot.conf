# Type Path Mode User Group Age Argument
# 用户级自动更新检查定时器: 全局启用(覆盖现有与未来用户)。
# linxira-update 包的 user preset 只在包安装时对 system 范围生效,
# 创建的用户不会自动应用; 此处在每次开机补建全局启用链接。
L /etc/systemd/user/default.target.wants/linxira-update.timer - - - - /usr/lib/systemd/user/linxira-update.timer
