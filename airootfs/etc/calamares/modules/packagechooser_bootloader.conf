---
mode: required
method: legacy
labels:
  step: "系统引导"
  step[en]: "System Boot"
default: grub
items:
  - id: grub
    name: "GRUB"
    description: "安装 Linxira OS 的受支持系统引导器。安装后的启动菜单会使用 Linxira OS 品牌，并在高级选项中提供 linux 与 linux-lts 双内核入口，适合多系统和回退启动。"
    description[en]: "Install the supported Linxira OS bootloader. The installed boot menu uses Linxira OS branding and exposes both linux and linux-lts kernels under advanced options for multi-boot and fallback startup."
